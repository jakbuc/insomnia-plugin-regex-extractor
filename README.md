# insomnia-plugin-regex-extractor

This plugin is inspired by official Insomnia response plugin. It adds new template tag "RegExp from response" which can be used to extract a value from a body response.
